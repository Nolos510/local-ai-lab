# Dashboard Inline SVG Charts

The model dashboard now renders zero-dependency inline SVG charts on Overview
and Compare using existing benchmark columns: total score, tokens/sec, RAM GB,
and per-axis score dimensions. The charts are server-rendered, theme through
existing CSS variables, and make no external network calls.
