# Dashboard Offline Icons

The model dashboard now renders its navigation and stat-card icons from a small
inline SVG helper instead of the Tabler CDN stylesheet. The vendored paths are
from Tabler Icons v3.26.0 under the MIT license, preserving the dashboard's
local-first behavior with no render-time network asset fetches.
